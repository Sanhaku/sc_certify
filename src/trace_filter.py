from src.loop import Loop
from src.sym_exec.trace import Trace


def get_growth_traces(traces: list(Trace), loops: list(Loop)) -> list(Trace):
    # TODO: get the growth traces from all the traces
    pass